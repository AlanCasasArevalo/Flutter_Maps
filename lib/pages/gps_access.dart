import 'package:flutter/material.dart';

class GpsAccessPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Container(child: Text('GpsAccess Page'),),
        )
    );
  }
}
